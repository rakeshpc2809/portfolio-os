[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"] # Formatting handles wrapping; ignore line length lint errors

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
